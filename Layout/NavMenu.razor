@using StubSite.Data
@using StubSite.Repo
@inject NavigationManager NavMan

@inject IJSRuntime JSRuntime
@implements IDisposable

<nav class="navbar navbar-expand-md navbar-dark bg-dark top-bar fade-in border-while-open navbar-grow flex-c-start">
    <div class="container-fluid">
        <a class="navbar-brand" href="">
            <img alt="Logo image" src="images/CompanyLogo.png" style="height:3em" />
        </a>
        <h2 class="navbar-toggler @NavButtonCssClass" style="border:none;font-size:xx-large;color:white">@Constants.CurPage</h2>
        <ToggleMenu OnToggle="ToggleNavMenu" />
        <div class="collapse navbar-collapse  fade-in-delay @NavBarCssClass" id="navbarCollapse" @onclick="ToggleNavMenu">
            <ul class="navbar-nav me-auto mb-2 mb-md-0">
                <li class="nav-item">
                    <NavLink class="nav-link" href="" Match="NavLinkMatch.All">
                        <span class="oi" aria-hidden="true"></span> Home
                    </NavLink>
                </li>
                @* <li class="nav-item">
                    <NavLink class="nav-link" href="services">
                        <span class="oi oi-plus" aria-hidden="true"></span> Services
                    </NavLink>
                </li> *@
                <li class="nav-item">
                    <NavLink class="nav-link" href="menu">
                        <span class="oi" aria-hidden="true"></span> Menu
                    </NavLink>
                </li>

                @* Specific Start *@
                <li class="nav-item">
                    <NavLink class="nav-link" href="events">
                        <span class="oi" aria-hidden="true"></span> Events & Rentals
                    </NavLink>
                </li>
                @* Specific End *@
                <li class="nav-item">
                    <NavLink class="nav-link" href="contact-us">
                        <span class="oi" aria-hidden="true"></span> Contact Us
                    </NavLink>
                </li>
                <li class="nav-item">
                    <NavLink class="nav-link" href="https://form.jotform.com/252374009495158">
                        <span class="oi" aria-hidden="true"></span> Apply Here!
                    </NavLink>
                </li>
            </ul>
        </div>

    </div>
    @if (NavMan.Uri.Trim('/').EndsWith("menu"))
    {
        <div class="flex-r-100" style="align-items:center;margin-top:1em;position:relative;bottom:0">
            <h5 style="margin-right:.25em;color: white;">
                Jump to Section:
            </h5>
            <FancySelect @bind-Value="SelCat" Style="width:fit-content">
                @foreach (string cat in Categories)
                {
                    <option value="@cat">@cat</option>
                }
            </FancySelect>
        </div>
    }
</nav>

@code {
    private bool collapseNavMenu = true;
    private string? NavBarCssClass => collapseNavMenu ? null : "show";
    private string? NavButtonCssClass => collapseNavMenu ? "collapsed" : null;

    List<string> Categories = new List<string>();

    string selCat { get; set; }
    string SelCat
    {
        get { return selCat; }
        set
        {
            if (string.IsNullOrEmpty(value))
                return;

            selCat = value;
            JumpToSection(selCat);
        }
    }

    protected override async Task OnInitializedAsync()
    {
        Constants.CurPageChanged += CurPageUpdated;

        Dictionary<string, List<Item>> Items = await MenuRepo.GetMenuItemsAsDictionary();
        Categories = Items.Keys.ToList();
    }
    private async void ToggleNavMenu()
    {
        collapseNavMenu = !collapseNavMenu;
    }

    private void CurPageUpdated(object sender, string cp)
    {
        StateHasChanged();
    }

    private async Task JumpToSection(string id)
    {
        await JSRuntime.InvokeVoidAsync("scrollToFragment", id);
    }

    public void Dispose()
    {
        Constants.CurPageChanged -= CurPageUpdated;
    }
}
